[
    {
        "content": "<p>I sometimes run into the problem, that a particular subgoal requires earlier proven subgoals. I always use <code>next</code> in between subgoals, but this prevents re-using the fact.  Is there a common solution? I could imagine: drop <code>next</code> and use the fact (which I haven't seen in any other code, I think) or start the overall proof using <code>proof -</code> state the facts using <code>have</code> and re-use them later.</p>",
        "id": 301680489,
        "sender_full_name": "Robert Soeldner",
        "timestamp": 1664553512
    },
    {
        "content": "<p><span class=\"user-mention silent\" data-user-id=\"403618\">Robert Soeldner</span> <a href=\"#narrow/stream/238552-Beginner-Questions/topic/Isar.20re-use.20facts.20and.60next.60/near/301680489\">said</a>:</p>\n<blockquote>\n<p>I sometimes run into the problem, that a particular subgoal requires earlier proven subgoals. I always use <code>next</code> in between subgoals, but this prevents re-using the fact.  Is there a common solution? I could imagine: drop <code>next</code> and use the fact (which I haven't seen in any other code, I think) or start the overall proof using <code>proof -</code> state the facts using <code>have</code> and re-use them later.</p>\n</blockquote>\n<p>I dropped the <code>next</code> in one particular case. In general, I don’t recommend it, because it leads to less structured proofs.</p>",
        "id": 301693226,
        "sender_full_name": "Wolfgang Jeltsch",
        "timestamp": 1664557212
    }
]